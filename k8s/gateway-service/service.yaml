apiVersion: v1
kind: Service
metadata:
  name: jwt-gateway-service
  namespace: app
spec:
  selector:
    app: jwt-gateway-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
