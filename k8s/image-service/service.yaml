apiVersion: v1
kind: Service
metadata:
  name: image-service
  namespace: app
spec:
  selector:
    app: image-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
  type: ClusterIP
