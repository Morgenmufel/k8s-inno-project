apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-init
  namespace: app
data:
  mongo-init.js: |
     db.createUser({
      user: "dbuser",
      pwd: "dbpassword",
      roles: [ {
        role: "readWrite",
        db: "activitydb"
      } ]
     });
      db.createCollection("activity");