apiVersion: v1
kind: Service
metadata:
  name: activity-service
  namespace: app
spec:
  selector:
    app: activity-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
  type: ClusterIP
